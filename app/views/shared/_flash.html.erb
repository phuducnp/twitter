<% flash.each do |type, msg| %>
  <script>
    Swal.fire({
      title: '<%= msg %>',
      <% if type == "notice" %>
        type: 'success',
      <% elsif type == "warning" %>
        type: 'alert',
      <% end %>
      timer: 1500
    })
  </script>
<% end %>
